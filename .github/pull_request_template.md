## What changed

<!-- Short sentence: what did you do? -->

## Issue tracking

<!-- When this PR is merged, GitHub can auto-close an issue if you use one of these in the PR title OR description (or a commit message):

- Closes #123
- Fixes #123

Example: `Closes #2` closes issue #2 when this merges. -->

## Checklist

- [ ] I ran `pip install -e .` locally and `lyric-lily --help` works
- [ ] Linked issues above with `Closes #…` / `Fixes #…` if this PR should close them on merge
